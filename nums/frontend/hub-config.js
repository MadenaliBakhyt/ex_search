// URL of the EXIM Search hub, used by the "← EXIM Search" header link.
// Edit this (or bind-mount your own copy over it) to point at wherever the
// hub is actually deployed -- see hub/README.md.
window.EXIM_HUB_URL = "http://localhost:8080";
